% Placeholder function for parsing GusT data.

% This function can be replaced with one that takes a raw GusT datafile and
% converts it to a struct with fields:
% 
%  dn - MATLAB datenum vector
%   t - temperature vector
%   p - pressure vector

function data = parse_GusT(f_in)
    data = [];
end
